# sample DarkIce configuration file, edit for your needs before using
# see the darkice.cfg man page for details

# this section describes general aspects of the live streaming session
[general]
duration        = 240        # duration of encoding, in seconds. 0 means forever
bufferSecs      = 5         # size of internal slip buffer, in seconds
reconnect       = yes       # reconnect to the server(s) if disconnected
realtime        = yes       # run the encoder with POSIX realtime priority
rtprio          = 3         # scheduling priority for the realtime threads

# this section describes the audio input that will be streamed
[input]
device          = /home/a.wav  # OSS DSP soundcard device for the audio input
sampleRate      = 16000     # sample rate in Hz. try 11025, 22050 or 44100
bitsPerSample   = 16        # bits per sample. try 16
channel         = 2         # channels. 1 = mono, 2 = stereo

[icecast2-0]
bitrateMode     = cbr       # average bit rate
format          = aac   # format of the stream: ogg vorbis
bitrate         = 16        # bitrate of the stream sent to the server
server          = 218.25.87.232
                            # host name of the server
port            = 8000      # port of the IceCast2 server, usually 8000
password        = sipsys123456    # source password to the IceCast2 server
mountPoint      = test  # mount point of this stream on the IceCast2 server
name            = XINWEN
                            # name of the stream
description     = This is XINWEN Radio
                            # description of the stream
url             = http://www.yourserver.com
                            # URL related to the stream
genre           = my own    # genre of the stream
public          = yes       # advertise this stream?


